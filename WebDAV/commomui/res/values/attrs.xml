<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="SwipeBackLayout">
        <attr name="edge_size" format="dimension" />
        <attr name="edge_flag">
            <enum name="left" value="0" />
            <enum name="right" value="1" />
            <enum name="bottom" value="2" />
            <enum name="all" value="3" />
        </attr>
        <attr name="shadow_left" format="reference" />
        <attr name="shadow_right" format="reference" />
        <attr name="shadow_bottom" format="reference" />
    </declare-styleable>
    <attr name="SwipeBackLayoutStyle" format="reference" />

    <declare-styleable name="SwitchButton">
        <!-- Drawable to use as the "thumb" that switches back and forth. -->
        <attr name="thumb" format="reference" />
        <!-- Drawable to use as the "thumb" that switches back and forth. -->
        <attr name="thumbUnable" format="reference" />
        <!-- Drawable to use as the "track" that the switch thumb slides within. -->
        <attr name="track" format="reference" />
        <!-- Text to use when the switch is in the checked/"on" state. -->
        <attr name="textOn" format="reference" />
        <!-- Text to use when the switch is in the unchecked/"off" state. -->
        <attr name="textOff" format="reference" />
        <!-- Color to use when the switch is in the unchecked/"off" state. -->
        <attr name="textOnColor" format="reference" />
        <!-- Color to use when the switch is in the unchecked/"off" state. -->
        <attr name="textOffColor" format="reference" />
        <!-- Amount of padding on either side of text within the switch thumb. -->
        <attr name="thumbTextPadding" format="dimension" />
        <!-- TextAppearance style for text displayed on the switch thumb. -->
        <attr name="switchTextAppearance" format="reference" />
        <!-- Minimum width for the switch component -->
        <attr name="switchMinWidth" format="dimension" />
        <!-- Minimum space between the switch and caption text -->
        <attr name="switchPadding" format="dimension" />
    </declare-styleable>
    <declare-styleable name="SwitchBtnTextAppearance">
        <!-- Text color. -->
        <attr name="android:textColor" />
        <!-- Size of the text. Recommended dimension type for text is "sp" for scaled-pixels (example: 15sp). -->
        <attr name="android:textSize" />
        <!-- Style (bold, italic, bolditalic) for the text. -->
        <attr name="android:textStyle" />
        <!-- Typeface (normal, sans, serif, monospace) for the text. -->
        <attr name="android:typeface" />
        <!-- Color of the text selection highlight. -->
        <attr name="android:textColorHighlight" />
        <!-- Color of the hint text. -->
        <attr name="android:textColorHint" />
        <!-- Color of the links. -->
        <attr name="android:textColorLink" />
    </declare-styleable>
    <attr name="SwitchButtonStyle" format="reference" />
    <attr name="SwitchBtnTextAppearance" format="reference" />

    <declare-styleable name="CentralTextAppearance">
        <attr name="android:textColor" />
        <attr name="android:textSize" />
        <attr name="android:textStyle" />
        <attr name="android:typeface" />
        <attr name="android:textColorHighlight" />
        <attr name="android:textColorHint" />
        <attr name="android:textColorLink" />
    </declare-styleable>
    <attr name="DpadStyle" format="reference" />
    <attr name="CentralTextAppearance" format="reference" />

    <declare-styleable name="DpadState">
        <attr name="state_pressed_center" format="boolean" />
        <attr name="state_pressed_left" format="boolean" />
        <attr name="state_pressed_top" format="boolean" />
        <attr name="state_pressed_right" format="boolean" />
        <attr name="state_pressed_bottom" format="boolean" />
    </declare-styleable>

    <declare-styleable name="TimePicker">
        <attr name="layout" format="reference" />
        <attr name="is24HourView" format="boolean" />
        <attr name="shadowColors" format="reference" />
        <attr name="shadowDrawable" format="reference" />
        <attr name="centerDrawable" format="reference" />
        <attr name="textColor" format="integer|reference" />
        <attr name="textSize" format="integer|reference" />
        <attr name="itemAllPadding" format="integer|reference" />
        <attr name="itemPaddingLeft" format="integer|reference" />
        <attr name="itemPaddingTop" format="integer|reference" />
        <attr name="itemPaddingRight" format="integer|reference" />
        <attr name="itemPaddingBottom" format="integer|reference" />
    </declare-styleable>
    <attr name="TimePickerStyle" format="reference" />

</resources>
